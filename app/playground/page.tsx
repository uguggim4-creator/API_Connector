          // 참조 이미지가 있으면 추가 (Data URI 형식 유지)\n          if (seedreamReferenceImages.length > 0) {\n            body.image_url = seedreamReferenceImages; // Data URI 형식 그대로 전송\n            body.style_strength = seedreamStyleStrength; // 참조 이미지 스타일 강도\n            console.log(`📸 참조 이미지 ${seedreamReferenceImages.length}개 전송 중...`);\n            console.log('Data URI 형식으로 전송 (data:image/...;base64,...)');\n            console.log('스타일 강도:', seedreamStyleStrength);\n            console.log('첫 번째 이미지 프리픽스:', seedreamReferenceImages[0]?.substring(0, 30));\n          }\n